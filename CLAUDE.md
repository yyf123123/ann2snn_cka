@AGENTS.md

# Claude Code Notes

Follow `AGENTS.md` as the single source of truth for repository rules and method descriptions.

For large changes, first produce a plan and wait for user confirmation before editing code.

Do not delete or move existing runnable code unless the replacement has been tested and approved.

The primary entry point is `resnet18_cifar10.py`.

The secondary entry point is `vgg16_cifar10.py` — do not delete it.

Read `docs/ai_context/` before making substantive edits:

1. `docs/ai_context/00_PROJECT_OVERVIEW.md`
2. `docs/ai_context/01_METHOD_TO_CODE_MAP.md`
3. `docs/ai_context/06_CURRENT_REPO_AUDIT.md`
